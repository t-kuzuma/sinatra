<form action="/memos/new" method="get">
  <input type="submit" value="追加" class="button">
</form>

<ul>
  <% @memos.each do |memo| %>
    <li><a href="/memos/<%= memo["id"] %>" class="top-title"><%= CGI.escapeHTML(memo["title"]) %></a></li>
  <% end %>
</ul>
